nickelulzian d00m renderer

BUILD INSTRUCTIONS:

1. Install GLFW and CGLM system-wide (Debian-Based):

sudo apt install libglfw3-dev libcglm-dev

2. Install the full repository recursively

git clone https://github.com/nickelulz/n00m/n00m.git --recursive

3. Enter the directory and run:

make run
